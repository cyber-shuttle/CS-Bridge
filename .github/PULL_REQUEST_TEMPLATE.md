## What changed

<!-- What this does and why. Closes #<issue> if it closes one. -->

## How it was tested

<!-- What you ran and against what: the commands below, plus the cluster and scheduler if this touches the
     launch or connect path. Webview changes have no automated coverage — attach a screenshot. -->

- [ ] `npm run check-types`, `npm run lint` and `npm test` pass locally
- [ ] `CHANGELOG.md` has an entry under `## [Unreleased]`, or nothing here is user-visible
